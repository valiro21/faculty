To install lighc library use:
```
cd /path/to/repo/an2/ag/libhc && make
```
 * You may need to enter your password for sudo commands

To include the library in your project use:
```
#include "hill_climbing.h"
```
